import * as React from 'react';
const styles = require('./DevDetails.scss');

export default class DevDetails extends React.PureComponent {
  render() {
    return <div className={styles.container} />;
  }
}
